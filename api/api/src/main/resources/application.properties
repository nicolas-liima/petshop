spring.application.name=petshop

# PostgreSQL (comentado temporariamente)
# spring.datasource.url= jdbc:postgresql://localhost:5432/petshop
# spring.datasource.username=postgres
# spring.datasource.password=miranha2018

# H2 Database (temporário)
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.h2.console.enabled=true

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

api.security.token.secret=${JWT_SECRET:my-secret-key-for-jwt-token-generation-12345}

file.upload-dir=./uploads/animais
